# flatmac

A minimal black-and-white Discourse theme using bundled Uncut Sans WOFF2 fonts.

- Body: Uncut Sans Regular (`400`)
- Headings: Uncut Sans Bold (`700`)
- Styling: primarily Discourse color scheme roles and CSS custom properties
- Controls: rounded buttons and inputs
